ARG MARIADB_VERSION
FROM mariadb:$MARIADB_VERSION
# ARG DB_HOST
ARG DB_ROOT_PASSWORD
ARG DB_USER
ARG DB_PASSWORD
ARG DB_NAME
ENV MARIADB_ROOT_PASSWORD=$DB_ROOT_PASSWORD
ENV MARIADB_USER=$DB_USER
ENV MARIADB_PASSWORD=$DB_PASSWORD
ENV MARIADB_DATABASE=$DB_NAME
ENV MARIADB_HOST=$DB_HOST
# COPY ../../docker/database/command.sh /home

EXPOSE 3306
# RUN service mariadb status
# RUN service mariadb start
# CMD mysql -h localhost -u $DB_USER -p$DB_PASSWORD -e "CREATE DATABASE $DB_NAME"
# RUN mysql -h $DB_HOST -u $MARIADB_USER -p$MARIADB_PASSWORD -e "CREATE DATABASE $DB_NAME"
# RUN echo $DB_NAME # Funciona
# RUN "echo $DB_NAME" # No funciona